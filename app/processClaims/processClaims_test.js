'use strict';

describe('myApp.processClaims module', function() {

  beforeEach(module('myApp.processClaims'));

  describe('processClaims controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var processClaimsCtrl = $controller('ProcessClaimsCtrl');
      expect(processClaimsCtrl).toBeDefined();
    }));

  });
});
