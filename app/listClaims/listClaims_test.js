'use strict';

describe('myApp.listClaims module', function() {

  beforeEach(module('myApp.listClaims'));

  describe('listClaims controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var listClaimsCtrl = $controller('ListClaimsCtrl');
      expect(listClaimsCtrl).toBeDefined();
    }));

  });
});
